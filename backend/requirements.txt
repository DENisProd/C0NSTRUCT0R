fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.36
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.12
alembic==1.13.2
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
asyncpg==0.29.0
email-validator==2.2.0
httpx==0.27.0
pytest==8.2.1
pytest-asyncio==0.23.7
asgi-lifespan==2.1.0
# Для разработки на SQLite драйвер PostgreSQL не требуется.
# Если используете PostgreSQL локально, установите один из вариантов вручную:
# - psycopg[binary]==3.*  (рекомендуется для SQLAlchemy: URL вида postgresql+psycopg://)
# - psycopg2-binary==2.*  (потребуется системный pg_config)
# Для SQLite (для разработки)
# sqlite3 встроен в Python
